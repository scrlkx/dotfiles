# dotfiles

Set of my personal dotfiles.

## What's inside

There are several cool things, with settings for git, hyper, zsh and more.

## Usage

1. `cd $HOME && git clone git@github.com:scrlkx/dotfiles.git`
2. `cd $HOME/dotfiles`
3. `sudo chmod +x install.sh`
4. `./install.sh`
